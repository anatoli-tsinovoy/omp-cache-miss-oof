name: CI

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - run: bun install --frozen-lockfile
      - name: Validate clean packed plugin import
        run: |
          bun pm pack --destination "$RUNNER_TEMP"
          archive="$RUNNER_TEMP/omp-cache-miss-oof-$(bun -p 'require("./package.json").version').tgz"
          host_version="$(bun -p 'require("./package.json").devDependencies["@oh-my-pi/pi-coding-agent"]')"
          ai_version="$(bun -p 'require("./package.json").devDependencies["@oh-my-pi/pi-ai"]')"
          mkdir "$RUNNER_TEMP/plugin-smoke"
          printf '{"name":"plugin-smoke","private":true}' > "$RUNNER_TEMP/plugin-smoke/package.json"
          cd "$RUNNER_TEMP/plugin-smoke"
          bun install "$archive" "@oh-my-pi/pi-coding-agent@$host_version" "@oh-my-pi/pi-ai@$ai_version"
          bun -e 'await import("omp-cache-miss-oof/src/index.ts")'
          mkdir "$RUNNER_TEMP/plugin-standalone-smoke"
          printf '{"name":"plugin-standalone-smoke","private":true}' > "$RUNNER_TEMP/plugin-standalone-smoke/package.json"
          cd "$RUNNER_TEMP/plugin-standalone-smoke"
          bun install "$archive"
          bun -e 'await import("omp-cache-miss-oof/src/index.ts")'
      - run: bun run check
      - run: bun test
      - run: bun run generate:sounds
      - run: git diff --exit-code -- sounds
